{
  "name": "dolpo.ai",
  "version": "1.0.0",
  "description": "Dolpo.ai website for private AI solutions on customer premises.",
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-router-dom": "^7.8.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.8"
  },
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --outdir=dist --jsx=automatic",
    "postbuild": "cp index.html dist/index.html",
    "test": "echo \"No tests\""
  }
}
